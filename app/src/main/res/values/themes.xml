<resources>
    <!-- Base application theme for light mode. The DayNight parent auto-flips
         android:colorBackground, colorSurface, textColorPrimary, and
         textColorSecondary based on the system theme; values-night/themes.xml
         only needs to override the brand-specific knobs. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/brand_secondary</item>
        <item name="colorSecondaryVariant">@color/brand_secondary_dark</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Dark brand bar with white system icons. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
